name: "🐛 Bug Report"
description: Report a crash, unexpected behavior, or instability in the modpack.
title: "[Bug]: "
labels: ["bug", "status: needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to report a bug! 🙏
        The modpack is in **Alpha** — our current focus is the stability of the corepack.
        ⚠️ **Before you continue**, please check that this bug hasn't already been reported in the [existing issues](../issues?q=is%3Aissue).

  - type: checkboxes
    id: prerequisites
    attributes:
      label: Prerequisites
      description: Please confirm the following before submitting.
      options:
        - label: I have searched the existing issues and this bug has not been reported yet.
          required: true
        - label: I am using a clean installation (no mods manually added to the modpack).
          required: true
        - label: I am playing on the latest version available on Modrinth.
          required: true

  - type: input
    id: modpack-version
    attributes:
      label: Modpack version
      description: The exact version number shown on Modrinth (e.g. 0.3.1-alpha).
      placeholder: "0.3.1-alpha"
    validations:
      required: true

  - type: dropdown
    id: environment
    attributes:
      label: Environment
      description: Where does the bug happen?
      options:
        - Singleplayer
        - Multiplayer (dedicated server)
        - Both
    validations:
      required: true

  - type: dropdown
    id: launcher
    attributes:
      label: Launcher
      options:
        - Modrinth App
        - Prism Launcher
        - CurseForge App
        - ATLauncher
        - MultiMC
        - Other
    validations:
      required: true

  - type: input
    id: ram
    attributes:
      label: Allocated RAM
      description: How much memory is allocated to the instance (in the launcher settings)?
      placeholder: "6 GB"
    validations:
      required: true

  - type: dropdown
    id: world-type
    attributes:
      label: World
      description: Does the bug happen on a fresh world or an existing one?
      options:
        - New world
        - Existing world (created on the current version)
        - Existing world (created on an older modpack version)
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: Bug description
      description: Clearly describe what is happening.
      placeholder: The game crashes when I place a conveyor belt next to a Poké Ball...
    validations:
      required: true

  - type: textarea
    id: reproduction
    attributes:
      label: Steps to reproduce
      description: A precise, numbered list of the actions that lead to the bug.
      placeholder: |
        1. Go to '...'
        2. Place '...'
        3. Interact with '...'
        4. Observe the crash / bug
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
      description: What should have happened normally?
    validations:
      required: true

  - type: input
    id: logs
    attributes:
      label: Logs / Crash report
      description: |
        ⚠️ **REQUIRED.** Paste a link to your logs via https://mclo.gs (recommended) or Pastebin.
        ❌ Do NOT post screenshots of your logs — they are unreadable and can't be copied.
      placeholder: "https://mclo.gs/xxxxxxx"
    validations:
      required: true

  - type: textarea
    id: additional
    attributes:
      label: Additional context
      description: Screenshots (of the game, not the logs), machine specs, performance mods (Sodium/Embeddium), etc.
    validations:
      required: false
